// Placeholder test — the app uses Hive + Riverpod which require full
// initialization before widget tests can run. Real tests live in
// integration_test/. This file exists only to satisfy the analyzer.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, equals(2));
  });
}
